apiVersion: v1
kind: Service
metadata:
  name: hello-python-service
spec:
  selector:
    app: hello-python
  ports:
  - protocol: "TCP"
    port: 8080
    targetPort: 8080
  type: NodePort


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: datagen-deployment
  labels:
    app.kubernetes.io/name: data-generation-example
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: data-generation-example
  template:
    metadata:
      name: datagen   
      labels:   
        app.kubernetes.io/name: data-generation-example   
    spec:   
      containers:   
      - image: lucaserf/obs:prints   
        name: hello-world   
        ports:   
        - containerPort: 8080
        volumeMounts:
        - name: varlog
          mountPath: /var/log
      volumes:
      - name: varlog
        emptyDir: {}